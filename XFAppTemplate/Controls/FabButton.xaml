<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:XFAppTemplate.Controls"
             x:Class="XFAppTemplate.Controls.FabButton">
    
    <ContentView.Content>
        <Grid RowSpacing="0" ColumnSpacing="0"
              HeightRequest="50" WidthRequest="50"
              VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <controls:ContentViewRoundedCorners CornerRadius="25" BackgroundColor="Olive" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" />
            <Button x:Name="Fab"
                    Text="&#xF254;"
                    Command="{Binding ChatCommand}"
                    Style="{StaticResource MaterialButton}" />
        </Grid>
    </ContentView.Content>
</ContentView>